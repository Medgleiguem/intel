flask==3.1.2
requests==2.32.5
jsonify==0.5
flask-cors==3.0.10
sentence-transformers==2.2.2
numpy==1.26.0
# GPT4All: install via pip from GitHub or a wheel if needed
# ai_answer: not on PyPI, remove or replace with the actual package you meant
